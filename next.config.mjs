/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "www.notion.so",
      "achen-dev.oss-cn-hongkong.aliyuncs.com",
    ],
  },
};

export default nextConfig;
